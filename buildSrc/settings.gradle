
dependencyResolutionManagement {
  // Reuse version catalog from the main build.
  versionCatalogs {
    libs {
      from(files("../gradle/libs.versions.toml"))
    }
  }
}

rootProject.name = 'build-framework'
